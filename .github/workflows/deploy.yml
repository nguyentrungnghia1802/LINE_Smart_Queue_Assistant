name: 🚫 CD (Temporarily disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: ⏸️ Production deployment disabled
    runs-on: ubuntu-latest
    steps:
      - name: ℹ️ Explain disabled state
        run: |
          echo "CD is intentionally disabled."
          echo "This workflow does not build images, push Docker images, or connect to a server."
